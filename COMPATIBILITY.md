# Compatibility

Public contracts are versioned with `major.minor.patch`. Open and Core (`NOMIVELA`) always share the same version number and tag `v<major>.<minor>.<patch>`. EE (`NOMIVELA-ee`) uses an independent version and tag. See the Core skill `.superpowers/skills/nomivela-release-versioning/SKILL.md`.
